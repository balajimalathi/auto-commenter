{
  "name": "auto-commenter",
  "version": "1.0.0",
  "description": "Marketing automation framework with Claude Skills and Agent0 CLI",
  "type": "module",
  "scripts": {
    "agent0": "cd agent0 && pnpm run dev --",
    "agent0:build": "cd agent0 && pnpm run build",
    "agent0:batch": "cd agent0 && pnpm run dev -- batch",
    "agent0:interactive": "cd agent0 && pnpm run dev",
    "install:all": "pnpm install && cd agent0 && pnpm install"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.31",
    "@playwright/mcp": "^0.0.63",
    "@playwright/test": "^1.58.1"
  }
}
